import { createStore } from "vuex";
import dashboard from "./modules/dashboard";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    dashboard,
  },
});
